name: Docker Image CI

on: [push]

jobs:

  build:
 
    runs-on: ubuntu-latest
 
    steps:
    - uses: actions/checkout@v1
      name: Checkout
    - uses: docker://analysiscenter1/ds-py3:cpu
      name: Some name
      run: ps -ef
